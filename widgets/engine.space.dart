import 'package:flutter/material.dart';

class EngineSpace extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 8.0,
      height: 8.0,
    );
  }
}

class EngineHalfSpace extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 4.0,
      height: 4.0,
    );
  }
}

class EngineBigSpace extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 24.0,
      height: 24.0,
    );
  }
}

class EnginePageSpace extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 12.0,
      height: 12.0,
    );
  }
}
